<div class="ajax-form-container">
    @using (Ajax.BeginForm("create", "RelationCate", new AjaxOptions()
{
    OnSuccess = "RelationCate_ajaxFormSuccess",
    OnFailure = "ajaxFormFailure"
}, new { formtitle = "添加RelationCate" }))
    {
        @Html.AntiForgeryToken()
        @Html.EditorForModel()
        <div class="form-field form-field-submit">
            <button type="submit" class="btn btn-green btn-save ">
                <i class="icon-button icon-btn-save"></i>
                保 存</button>
        </div>
    }
</div>

