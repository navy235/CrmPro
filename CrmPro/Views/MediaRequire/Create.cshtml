<div class="ajax-form-container">
    @using (Ajax.BeginForm("create", "MediaRequire", new AjaxOptions()
{
    OnSuccess = "MediaRequire_ajaxFormSuccess",
    OnFailure = "ajaxFormFailure"
}, new { formtitle = "添加MediaRequire" }))
    {
        @Html.AntiForgeryToken()
        @Html.EditorForModel()
        <div class="form-field form-field-submit">
            <button type="submit" class="btn btn-green btn-save ">
                <i class="icon-button icon-btn-save"></i>
                保 存</button>
        </div>
    }
</div>

